/*
  高度样式根据外部定义自动撑开
*/
.deleteItem{
  width:100%;
  height:100%;
  box-sizing:border-box;
}
movable-area {
  position: relative;
  box-sizing:border-box;
  /* height: 120rpx; */
  min-height:60rpx;
  height:100%;
  /* 宽度为总宽度剪去删除按钮的宽度,这里默认删除按钮宽度为120rpx,若需要更改，可在调用时自己控制movable-area的样式*/
  width: calc(100vw - 120rpx);
}

movable-view {
  position: relative;
  height: 100%;
  min-height:60rpx;
  width: 100vw;
  background: #fff;
  z-index:99;
}

